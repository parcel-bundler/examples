# Simple Example of Parcel with various frameworks

Example code of ParcelJs with VueJs Single file component and ReactJs

# Run

* Run `npm i`
* Run `npm start`

# Production Build

* Run `npm run build`
* Run `npm run serve`
